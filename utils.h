#ifndef APOLLO_UTILS_H
#define APOLLO_UTILS_H

#include <string>
#include <vector>

namespace Apollo {

std::string toHexStr(const std::vector<unsigned char> &, const std::string & = " ");

} // namespace Apollo

#endif // APOLLO_UTILS_H
